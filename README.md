# pawstamp-landing

발도장 앱 랜딩 페이지. Cloudflare Pages로 `pawstamp.rubi-on.com`에 배포됩니다.

## 구조
- `index.html` — 단일 정적 페이지
- 별도 빌드 과정 없음 (Cloudflare Pages 설정에서 build command 비워두기)

## 배포
GitHub `main` 브랜치 push → Cloudflare Pages 자동 배포.

## 수정 시 주의
- 개인정보처리방침 노션 URL은 `index.html`의 `#privacy-link` 앵커에 적용
- 문의 이메일: `tjrwls883@gmail.com`
